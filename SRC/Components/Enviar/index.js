import React from "react";
import { Button, View } from "react-native";

export default function Enviar(){
    return (
        <View>
            <Button
            title="Enviar"
            />
        </View>
    );
}